"use client";

// RainbowKit's connect button: MetaMask + other injected wallets + WalletConnect
// QR (mobile). Re-exported so the rest of the app imports one stable name.
export { ConnectButton } from "@rainbow-me/rainbowkit";
