# Copy to contracts/.env, then I run the deploy.
#   Copy-Item .env.example .env

# A funded Aeneid testnet wallet private key. MUST start with 0x.
# This is the deployer; it needs a little testnet IP for gas.
PRIVATE_KEY=0x

# Aeneid RPC (default is fine).
RPC_URL=https://aeneid.storyrpc.io
